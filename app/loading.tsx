export default function Loading() {
  return (
    <div className="mx-auto flex min-h-screen max-w-[520px] items-center justify-center px-5">
      <p className="text-sm font-semibold text-[var(--muted)]">불러오는 중</p>
    </div>
  );
}
